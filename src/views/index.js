const { analyticsView } = require("./analytics-view");
const { homeView } = require("./home-view");
const { generalStatusView } = require("./generalStatus-view");

module.exports = { analyticsView, homeView, generalStatusView };
